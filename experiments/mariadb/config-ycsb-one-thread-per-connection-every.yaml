name: "YCSB one-thread-per-connection"

_reference:
  - "config-common.yaml"
  - "config-one-thread-per-connection-every.yaml"

benchmark:
  name: "ycsb"
  config: "benchbase-config/10-terminal/benchbase-ycsb-1200-config.xml"

